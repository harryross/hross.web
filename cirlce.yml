machine:
  timezone:
    Pacific/Auckland

  node:
    version: 0.12.7
dependencies:
    override:
        - bundle install
        - sudo pip install awscli

deployment:
  aws:
  branch: master
    commands:
      - gulp build
      - /bin/bash publish.sh
